# 인수인계 — 음성 수집 시스템 (voice-collector / borami-xvarm-broker / admin-fe)

> 작성: 2026-09-17 16:50 · 직전 문서: `인수인계_0915142910.md` (이하 "0915 문서")
> 대상: 다른 Claude 계정(또는 다음 담당자)이 **이 문서만 읽고** 바로 이어서 작업할 수 있게 썼습니다.
> 0915 문서의 1장(프로젝트 개요·2트랙 개념)·3.4(EXEC_ID 규약)·8장(미확정 Q1~Q15)은 그대로 유효하므로 반복하지 않습니다.
> **0장 → 1장 → 2장 순서로 읽으십시오.** 나머지는 막힐 때 찾아보면 됩니다.

---

## 0. 지금 당장 해야 할 일

| 순서 | 내용 | 상태 | 어디 |
|---|---|---|---|
| 1 | **로컬 재검증** — 브로커 재기동 후 [시뮬레이션 데이터 생성] → [전체 실행(10분 주기)] 이 접견 2·전화 2 성공하는지 | 코드 수정 완료 · **검증 미완** | 1.1 |
| 2 | **개발계 배포** — voice-collector·브로커 차트를 `data-pipeline` 에 배포하고 admin-fe 화면에서 API 가 붙는지 | **미배포** (Service 자체가 없음) | 1.2 |
| 3 | HelmChart ConfigMap 에 `BROKER_OUTPUT_DIR` 등 새 환경변수 반영 여부 결정 | 미착수 | 1.2 |
| 4 | 0915 문서 6.2(비식별 커넥터 잔재 제거)는 **여전히 미착수·설계 충돌 미해결** | 보류 | 0915 §6.2 |

### 작업 규칙 (사용자가 명시한 것 — 반드시 지킬 것)

- **커밋 메시지에 `Co-Authored-By:` 등 Claude 관련 표기를 절대 넣지 않는다.**
- 서비스 소스는 `feature/*` 에서 작업 → `dev` 로 머지 → push (Jenkins 가 dev 를 배포). 헬름 차트는 `master`.
- 사용자의 IntelliJ 인스턴스(브로커 8082 · 수집기 8085 · 로그 컬렉터 8090)는 **절대 죽이지 않는다.** 검증용은 18085/18082 등 다른 포트로 띄우고 끝나면 정리한다.
- 개발계 보라미 DB 포트포워딩은 **사용자가 수동으로** `localhost:15433` 에 연다. 포트를 바꾸지 말 것(25432 로 바꾸라는 옛 지시는 철회됨).
- `ref/` 는 `.git/info/exclude` 로 로컬 제외돼 있다 → 문서/SQL 을 올릴 땐 `git add -f ref/…`.

---

## 1. 현재 열려 있는 문제 2건 — 원인과 조치 상태

### 1.1 로컬: `전체 실행(10분 주기)` 에서 접견 2건 실패

```
MEET/SIM-MEET-006 [COLLECT] — IllegalStateException: 브로커가 파일을 우리 수신 폴더 밖에 만들었다
  — 산출 C:\Projects\data_voice-collector-x2daarjxe4\work\voice_raw\meet\mock_meet_006.m4a
  / 수신 C:\k8s\voice_collector\esb\meet
```

**원인**: 수집기의 접견 수신 폴더가 이번 작업으로 `{ROOT_DIR}/esb/meet` (= `C:/k8s/voice_collector/esb/meet`) 로 바뀌었는데,
사용자의 IntelliJ 브로커(8082, local 프로파일)는 **옛 기본값** `../data_voice-collector-x2daarjxe4/work/voice_raw/meet` 에 파일을 만들었다.
(전화는 브로커를 타지 않아 2건 성공 — 0915 문서 1장의 2트랙 개념 그대로.) 즉시 실패로 잡힌 것은 `7c9d63d` 의 "경로 어긋남 즉시 실패" 가 의도대로 동작한 것이다.

**조치(완료, 브로커 `dev` `181cebe`)**: 브로커 기본값을 새 표준으로 바꿨다.

| 파일 | 변경 |
|---|---|
| broker `application-local.yml` | `output-dir: ${BROKER_OUTPUT_DIR:C:/k8s/voice_collector/esb/meet}` |
| broker `application-dev.yml` | `output-dir: ${BROKER_OUTPUT_DIR:/k8s/voice_collector/esb/meet}` (예전 `/k8s/xvarm_out`) |
| broker README | 안내 문구 갱신 |

**남은 일(사용자 또는 다음 담당자)**:
1. IntelliJ 에서 브로커(8082)를 **재기동** (dev 최신 pull). 환경변수 `BROKER_OUTPUT_DIR` 를 Run Configuration 에 넣어 뒀다면 지우거나 `C:/k8s/voice_collector/esb/meet` 로.
2. 시뮬레이터 `[브로커 연결 확인]` → `pathMatched: true` 확인.
3. `[시뮬레이션 데이터 초기화]` → `[시뮬레이션 데이터 생성]` → `[전체 실행(10분 주기)]` → 접견 2·전화 2 성공, 실패 0.
   (초기화를 먼저 하는 이유: 실패한 006/007 의 멱등 표식은 안 남지만, 브로커가 옛 폴더에 만든 파일이 남아 있어도 무관하게 깨끗이 시작하려고.)

> 이 검증은 제가 하지 못했습니다(사용량 한도). 마지막으로 제가 직접 확인한 성공은 16:00 경 **제가 띄운** 브로커(18082, `BROKER_OUTPUT_DIR=C:/k8s/voice_collector/esb/meet`)와 수집기(18085) 조합에서 주기 4/4 · 일배치 10/10 이었습니다.

### 1.2 개발계(admin-fe) 화면: ROOT_DIR 이 Windows 식으로 보이고 상세 디렉터리가 안 나옴

**원인**: 버그가 아니라 **voice-collector 가 개발계에 아직 배포돼 있지 않다.**
`kubectl get svc -n data-pipeline` 에 `voice-collector-x2daarjxe4` · `borami-xvarm-broker-1joiuorqhl` Service 가 없다
(HelmChart `a2cd599` 커밋만 있고 클러스터 적용 전). 그래서 화면이 `/api/v1/voice/status` → `/voice/api/v1/voice/status` 둘 다 실패하고,
ROOT_DIR 칸에 보이는 `C:/k8s/voice_collec…` 는 **input 의 placeholder** ("C:/k8s/voice_collector 또는 /k8s/voice_collector" 가 잘려 보인 것)이며
디렉터리 6종 목록은 서버 값이 없어 그리지 못한 것이다.

**조치(완료)**:
- admin-fe nginx `/voice/` 를 `resolver` + 변수 upstream 으로 바꿔 **voice-collector 가 없어도 nginx 가 뜨게** 했다 (admin-fe `dev` `7e200cd`).
  이전에는 고정 호스트명 때문에 `[emerg] host not found in upstream` 으로 새 파드가 CrashLoop 되어 Lens 에 파드가 2개(구·신)로 보였다.
- 시뮬레이터 HTML: API 두 후보가 모두 죽어 있으면 헤더 "환경" 줄에 **빨간 안내**(시도한 URL · 배포/기동 확인 요망 · "아래 값은 빈 화면") 를 띄우게 했다 — 이 문서와 같은 커밋.

**남은 일**:
1. **voice-collector 차트 배포** (`data_HelmChart/pipeline/voice-collector-x2daarjxe4`, ns `data-pipeline`, Service `voice-collector-x2daarjxe4:8080`) 와 **브로커 차트 배포**. 배포 경로(투라코/Jenkins)는 사용자가 안다.
2. 차트 ConfigMap 에 넣을지 결정할 환경변수 — 전부 비우면 코드가 자동 감지하므로 **필수는 아니다**:

   | 변수 | 자동 감지값(K8s) | 언제 넣나 |
   |---|---|---|
   | `VOICE_BASE_DIR` | `/k8s/voice_collector` | PVC 마운트 경로가 다를 때 |
   | `VOICE_BROKER_BASE_URL` | `http://borami-xvarm-broker-1joiuorqhl:8080` | 브로커 Service 명이 다를 때 |
   | `LOG_COLLECTOR_BASE_URL` | `http://log-collector-a2z96kgyrm:8080/logc` | ⚠ 사용자는 `data-log-collector-a2z96kgyrm:8090` 이라 했으나 차트의 Service 는 `log-collector-a2z96kgyrm`·8080 이라 차트값을 썼다. 틀리면 이 변수로 덮어쓸 것 |
   | (브로커) `BROKER_OUTPUT_DIR` | `/k8s/voice_collector/esb/meet` | 두 파드가 같은 PVC 를 보게 하려면 voice-collector PVC 를 브로커에도 마운트해야 한다 — 차트에 아직 없음 |

3. 배포 후 `https://kcais-admin.twolinecloud.com/voice_collector_simulator.html` 에서 헤더 "환경" 줄이 `K8S · Linux · profile dev · ROOT /k8s/voice_collector` 로 뜨고 디렉터리 6종이 보이면 끝.
   그 다음 admin-fe 파드 로그에 `[emerg]` 가 없는지, Lens 에 파드가 1개만 남는지 확인.

---

## 2. 0915 문서 이후 진행된 작업 (2026-09-15 ~ 09-17)

0915 문서 6장의 지시 3건은 **모두 완료**됐고, 이후 사용자 지시로 큰 작업이 5차례 더 있었다. 전부 `dev` 에 머지·push 됨.

### 2.1 voice-collector (`dev` HEAD `8fd756d`, 테스트 **94/94**)

| 커밋 | 내용 |
|---|---|
| `7c9d63d` | 0915 §6.1·6.3 완료 — 로컬 8085, 프로파일 없으면 local, 브로커 주소 라디오, 실행 스피너, **브로커 경로 어긋남 즉시 실패** |
| `3a3751e` | T4 적재 요청을 로그 컬렉터 스펙에 맞춤(`recFileId` NOT NULL) |
| `ca27e3d` | T2 단계 로그 COLLECT·ANALYZE 복원 · **STT 출력을 배치(EXEC_ID) 폴더에 저장** · 디렉터리 프로퍼티 분리 · `PiiResidueAuditor` 제거 |
| `cb4d57c` | 시뮬레이터 디렉터리 설정 UI · API/Response JSON 표시 · STT 텍스트 표 · 재처리(retry) 시나리오 |
| `49c7b0f` | Mock 축소 · H2 재적재 API · **아코디언 UI** · DIRECT_JDBC 원본 SQL 출력(`ref/borami_direct_jdbc_queries*.sql`) |
| `69c9d34` | **시뮬레이션 데이터 생성/초기화**(`SimulationDataService`: DB 메타 + 물리 더미 파일) · XVARM 연동 모드(MOCK_DEV/REAL) · 누락 테이블 DDL `ref/borami_missing_tables.sql` |
| `151356f` | **DataSource 라우팅** — `BoramiDbRouter`(MOCK→H2 인메모리, DIRECT_JDBC→개발계 PostgreSQL `localhost:15433`), DB probe, DB_ERROR 안내 |
| `8eccbd9` | **OS 별 ROOT_DIR 자동 감지 · 표준 디렉터리 6종 · 환경 프리셋 `GET /api/v1/voice/config` · admin-fe 동기화 스크립트 · 시뮬레이션 14건 · 헤더/버튼 정리** |
| `8fd756d` | `scripts/sync-admin-fe.ps1` UTF-8 BOM(PowerShell 5.1 한글 커밋 메시지 깨짐) |

핵심 개념(새로 생긴 것):

- **`DeployEnvPreset`** — 기동 시 OS+profile 로 `LOCAL`/`K8S` 판정. ROOT_DIR(Windows `C:/k8s/voice_collector` · Linux `/k8s/voice_collector`), 브로커·로그 컬렉터 URL 프리셋. **설정(환경변수)이 비어 있을 때만** 적용.
- **`VoiceDirState`** — ROOT_DIR 아래 표준 6종. 기동·시뮬레이션 생성/초기화·경로 변경 때 `ensureDirs()` 로 자동 생성.

  ```
  {ROOT}/xvram/original_voice_files   XVARM 접견 원본(더미 파일 15개가 여기)   voice.dirs.xvarm-original
  {ROOT}/esb/meet · esb/phone         ESB 수신 — ★ 브로커 BROKER_OUTPUT_DIR 과 같아야   receive-meet / receive-phone
  {ROOT}/xvram/decoding               복호화·작업(멱등 표식 .processed/)         work
  {ROOT}/xenon/meet/{execId} · xenon/phone/{execId}   최종 STT 저장             output-meet / output-phone
  ```
- **시뮬레이션 데이터 14건** = 일배치 접견 5·전화 5 + 주기 접견 2·전화 2. 키 `SIM-MEET-001…007`, `SIM-PHONE-001…007`. 더미 파일 `mock_meet_00i.m4a`·`mock_phone_00i.wav`(+ `mock_phone_003.stt.txt`).
  `MOCK` 은 H2(기동 시 자동 시딩), `개발계 DB` 는 [시뮬레이션 데이터 생성] 을 눌러야 INSERT 되고 XVARM 모드가 `MOCK_DEV` 면 `sm`·`xvarm` 스키마 누락 테이블을 먼저 만든다.
- **헤더 버튼** `[Swagger][시뮬레이션 데이터 생성][시뮬레이션 데이터 초기화][상태 새로고침]`. 초기화 = `DELETE /api/v1/mock/test-data` = TST 배치 이력(로그 컬렉터) + STT 출력 폴더 + SIM 행 + 더미 파일 전부.
- **② 배치 버튼 순서** 전체 실행(10분 주기) · 전체 실행(일배치) [기본색] · 접견만 · 전화만 [옅은 색].
- **시뮬레이터 API base 자동 감지** `''` → `/voice` (`?api=http://host:port` 로 강제). admin-fe 를 통해 뜨면 `/voice`.

### 2.2 borami-xvarm-broker (`dev` HEAD `181cebe`)

| 커밋 | 내용 |
|---|---|
| `848c839` | 프로파일 없으면 local · 응답 `filePath` 절대경로(수집기의 즉시 실패 판정용) |
| `181cebe` | **출력 폴더 기본값을 `{ROOT_DIR}/esb/meet` 로** (1.1 의 조치) |

### 2.3 admin-fe (`service_admin-fe-iqgor1oiru`, 작업 브랜치 `feature/deid-demo`, `dev` HEAD `7e200cd`)

- `public/voice_collector_simulator.html` — 수집기 레포의 HTML 과 **동일 파일**. `scripts/sync-admin-fe.ps1`(`.sh`) 이 복사 → `git add public/voice_collector_simulator.html custom-nginx.conf` → commit → push(현재 브랜치). **HTML 을 고치면 이 스크립트를 돌린 뒤 admin-fe `feature/deid-demo` → `dev` 머지·push** 까지 해야 배포된다.
- `custom-nginx.conf` — `location = /voice_collector_simulator.html`(정적) + `location /voice/`(resolver + `$voice_backend` 변수 + `rewrite ^/voice/(.*)$ /$1 break;` → `voice-collector-x2daarjxe4.data-pipeline.svc.cluster.local:8080`).
- admin-fe `dev` 에는 다른 개발자 커밋이 계속 올라온다(09-17 에 12개). 머지 전 `git pull --ff-only origin dev` 부터.

### 2.4 로그 컬렉터·HelmChart

- 로그 컬렉터(`data_log-collector-a2z96kgyrm`)는 이번 기간 변경 없음. 검증에 쓴 `DELETE /logc/api/v1/logs/test-data` 로 TST 만 지운다.
- HelmChart `a2cd599`(voice-collector·브로커 개발계 설정) 는 **커밋만, 미배포**. 0915 §5.5 의 "투라코 재생성이 설정을 지운다" 문제는 그대로다.

---

## 3. 로컬 검증 절차 (내가 쓴 방식 그대로)

```
1) 사용자의 IntelliJ 인스턴스(8082/8085/8090)를 건드리지 말고, 검증용은 별 포트로:
   JAVA_HOME=C:\Users\이현구\.jdks\temurin-17.0.19
   mvn = "C:\Program Files\JetBrains\IntelliJ IDEA 2026.1.4\plugins\maven\lib\maven3\bin\mvn"  -o -Denforcer.skip=true
   (0915 §2.1 의 Maven 3.9.9/-Dmaven.repo.local 방식도 여전히 유효 — 오프라인 -o 는 이제 캐시가 차서 된다)
2) 브로커:  java -jar target/borami-xvarm-broker-1joiuorqhl.jar --spring.profiles.active=local --server.port=18082
           (181cebe 이후엔 BROKER_OUTPUT_DIR 불필요. 이전 jar 면 BROKER_OUTPUT_DIR=C:/k8s/voice_collector/esb/meet)
3) 수집기:  java -Dfile.encoding=UTF-8 -jar target/voice-collector-x2daarjxe4.jar --spring.profiles.active=local --server.port=18085
           → 기동 로그에 LOCAL/Windows · ROOT C:/k8s/voice_collector · 폴더 6종 생성 · H2 시딩 14건
4) 로그 컬렉터 8090 이 없으면 T1/T2/T4 가 "연결 실패" 경고만 내고 배치는 완주한다. 필요하면 로그 컬렉터 레포에서 임베디드 PG 로 띄운다(메모리 파일 local-verification-setup 참조)
5) http://localhost:18085/voice_collector_simulator.html
   [브로커 연결 확인] pathMatched → [시뮬레이션 데이터 생성] → [전체 실행(10분 주기)] 2·2 → [전체 실행(일배치)] 5·5 → [시뮬레이션 데이터 초기화]
6) 끝나면 18085/18082 프로세스와 임베디드 PG 를 정리
```

시뮬레이터 HTML 을 파이썬으로 고칠 때의 함정(0915 §2.4)은 여전하다. 한글·백슬래시가 든 패치는 heredoc 대신 **스크립트 파일로 써서** 실행하고, 고친 뒤 브라우저 콘솔 `new Function(js)` 로 문법을 확인할 것.

---

## 4. 주요 파일 위치 (이번 기간에 생기거나 크게 바뀐 것)

```
voice-collector
├─ config/DeployEnvPreset.java        환경 자동 감지 · 프리셋 상수 (BROKER_K8S, LOGC_K8S …)
├─ config/VoiceDirState.java          ROOT_DIR 표준 6종 · ensureDirs · presets
├─ config/BoramiDbRouter.java, BoramiDataSourceConfig.java   H2 ↔ 개발계 PostgreSQL 라우팅
├─ source/SimulationDataService.java  시뮬레이션 14건 + 더미 파일 · clean
├─ source/SimulationSeedRunner.java   기동 시 H2 시딩 (voice.sim.seed-on-startup)
├─ controller/VoiceBatchController.java   /status · /config · 배치
├─ controller/VoiceMockController.java    /mock/sim-data · /mock/test-data · /mock/dirs · /mock/db/probe
├─ resources/static/voice_collector_simulator.html   시뮬레이터 (admin-fe public/ 과 동일)
├─ scripts/sync-admin-fe.ps1 / .sh    admin-fe 동기화
└─ ref/  borami_missing_tables.sql · borami_direct_jdbc_queries(_주석).sql · 인수인계_*.md  (git 로컬 제외 → add -f)
```

README.md(수집기)가 이 전부를 사용자 관점으로 설명한다 — "환경 자동 감지", "표준 디렉터리 6종", "시뮬레이션 데이터 — DB 메타 14건", "시뮬레이터 화면 구성" 절.

---

## 5. 마지막 상태 스냅샷 (2026-09-17 16:50)

| 레포 | 브랜치 | HEAD | 미커밋 |
|---|---|---|---|
| `data_voice-collector-x2daarjxe4` | `feature/voice-collector-migration` = `dev` | `8fd756d` + 이 문서 커밋 | 없음 |
| `data_borami-xvarm-broker-1joiuorqhl` | `dev` | `181cebe` | 없음 |
| `service_admin-fe-iqgor1oiru` | `feature/deid-demo` → `dev` | `7e200cd` (+ 이 HTML 동기화 커밋) | 없음 |
| `data_HelmChart` | `master` | `a3cb88f` (voice 관련은 `a2cd599`) | 없음 · **미배포** |

- 수집기 `mvn test` 94/94 통과 (09-17 16:00).
- 개발계 K8s: admin-fe 만 떠 있음. voice-collector·브로커 Service 없음 (`kubectl get svc -n data-pipeline` 로 확인).
- 접속 URL: 로컬 `http://localhost:8085/voice_collector_simulator.html`, 개발계 `https://kcais-admin.twolinecloud.com/voice_collector_simulator.html` (voice-collector 배포 후 동작).
